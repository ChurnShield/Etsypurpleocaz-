Run the full test suite for this project.

## Steps
1. Run `pytest tests/ -v` to execute all tests with verbose output
2. Report any failures with file paths and line numbers
3. If tests fail, identify the root cause and suggest fixes
4. Do NOT commit code until all tests pass
