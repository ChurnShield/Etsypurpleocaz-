{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && git fetch origin main 2>/dev/null && behind=$(git rev-list --count HEAD..origin/main 2>/dev/null || echo 0) && if [ \"$behind\" -gt 0 ]; then echo \"[SYNC] $behind new commits on remote — run: git pull origin main\"; else echo \"[SYNC] Up to date with remote\"; fi"
          }
        ]
      }
    ]
  }
}
